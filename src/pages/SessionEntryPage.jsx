/**
 * Enhanced Session Entry Page
 * Now uses SessionEntryForm which handles its own layout, header, and UI
 */
import SessionEntryForm from '../components/session/SessionEntryForm'

export default function SessionEntryPage() {
  return <SessionEntryForm />
}
